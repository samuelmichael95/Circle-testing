node{
   stage('SCM Checkout'){
     git 'https://github.com/samuelmichael95/Circle-testing.git'
   }
   stage('compile-Package'){
      // Get maven home path
      def mvnHome = tool name: '', type: 'maven'
      sh "${mvnHome}/bin/mvn package"
   }

}
